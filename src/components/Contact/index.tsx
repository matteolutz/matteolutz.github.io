import {FC} from "react";

const Contact: FC = () => (
    <section className="section section-contact" id="contact">
        <h1 className="numbered-heading">Contact</h1>
        ...
    </section>
);

export default Contact;